import styled from "styled-components";

const Title = styled.div`
  font-weight: bold;
  font-size: 38px;
  color: white;

  @media (max-width: 700px) {
    font-size: 24px;
  }
`;

export { Title };
